name: 'eolien_pipeline'
version: '1.0.0'
config-version: 2

profile: 'eolien_pipeline'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  eolien_pipeline:
    staging:
      +schema: staging
      +materialized: view
    marts:
      +schema: mart
      +materialized: table
